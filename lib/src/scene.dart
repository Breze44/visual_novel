import 'package:flutter/widgets.dart';

class Scene extends StatelessWidget {
  const Scene({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text("scene");
  }
}
